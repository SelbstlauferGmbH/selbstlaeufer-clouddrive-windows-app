<Window x:Class="CloudDrive.App.Views.RemoteTargetChangeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Icon="pack://application:,,,/Resources/selbstlaeufer-cloud-app-icon-1024px.png"
        Width="620"
        Height="390"
        MinWidth="560"
        MinHeight="360"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Theme.SurfaceBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText"
                   Grid.Row="0"
                   FontSize="20"
                   FontWeight="SemiBold"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource Theme.TextPrimaryBrush}"/>

        <TextBlock x:Name="SummaryText"
                   Grid.Row="1"
                   Margin="0,10,0,16"
                   FontSize="13"
                   LineHeight="20"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource Theme.TextSecondaryBrush}"/>

        <Border Grid.Row="2"
                Padding="14"
                BorderBrush="{DynamicResource Theme.BorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Background="{DynamicResource Theme.SurfaceAltBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="132"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="OldTargetLabel"
                           Grid.Row="0"
                           Grid.Column="0"
                           Margin="0,0,12,8"
                           FontSize="11"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource Theme.TextSecondaryBrush}"/>
                <TextBlock x:Name="OldTargetText"
                           Grid.Row="0"
                           Grid.Column="1"
                           Margin="0,0,0,8"
                           FontSize="12"
                           TextTrimming="CharacterEllipsis"
                           Foreground="{DynamicResource Theme.TextPrimaryBrush}"/>

                <TextBlock x:Name="NewTargetLabel"
                           Grid.Row="1"
                           Grid.Column="0"
                           Margin="0,0,12,0"
                           FontSize="11"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource Theme.TextSecondaryBrush}"/>
                <TextBlock x:Name="NewTargetText"
                           Grid.Row="1"
                           Grid.Column="1"
                           FontSize="12"
                           TextTrimming="CharacterEllipsis"
                           Foreground="{DynamicResource Theme.TextPrimaryBrush}"/>
            </Grid>
        </Border>

        <TextBlock x:Name="WarningText"
                   Grid.Row="3"
                   Margin="0,16,0,0"
                   FontSize="12"
                   LineHeight="19"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource Theme.TextSecondaryBrush}"/>

        <Grid Grid.Row="4" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="CancelButton"
                    Grid.Column="1"
                    MinWidth="96"
                    Padding="14,8"
                    Margin="0,0,10,0"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
            <Button x:Name="KeepButton"
                    Grid.Column="2"
                    MinWidth="150"
                    Padding="14,8"
                    Margin="0,0,10,0"
                    Click="KeepButton_Click"/>
            <Button x:Name="ResetButton"
                    Grid.Column="3"
                    MinWidth="160"
                    Padding="14,8"
                    Background="{DynamicResource Theme.DangerStrongBrush}"
                    Foreground="White"
                    BorderBrush="{DynamicResource Theme.DangerStrongBorderBrush}"
                    Click="ResetButton_Click"/>
        </Grid>
    </Grid>
</Window>
