<Window x:Class="CloudDrive.App.Views.RemoteDeleteDecisionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Icon="pack://application:,,,/Resources/selbstlaeufer-cloud-app-icon-1024px.png"
        Width="580"
        Height="430"
        MinWidth="520"
        MinHeight="360"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Theme.SurfaceBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText"
                   Grid.Row="0"
                   FontSize="20"
                   FontWeight="SemiBold"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource Theme.TextPrimaryBrush}"/>

        <TextBlock x:Name="SummaryText"
                   Grid.Row="1"
                   Margin="0,10,0,18"
                   FontSize="13"
                   LineHeight="20"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource Theme.TextSecondaryBrush}"/>

        <Border Grid.Row="2"
                BorderBrush="{DynamicResource Theme.BorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Background="{DynamicResource Theme.SurfaceAltBrush}">
            <ListBox x:Name="ItemsList"
                     BorderThickness="0"
                     Background="Transparent"
                     Foreground="{DynamicResource Theme.TextPrimaryBrush}"
                     ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="12,10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0"
                                       Text="{Binding DisplayName}"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis"
                                       Foreground="{DynamicResource Theme.TextPrimaryBrush}"/>
                            <TextBlock Grid.Row="1"
                                       Margin="0,4,0,0"
                                       Text="{Binding Location}"
                                       FontSize="11"
                                       TextTrimming="CharacterEllipsis"
                                       Foreground="{DynamicResource Theme.TextSecondaryBrush}"/>
                            <TextBlock Grid.Row="2"
                                       Margin="0,4,0,0"
                                       Text="{Binding RemotePathDisplay}"
                                       FontSize="11"
                                       TextTrimming="CharacterEllipsis"
                                       Foreground="{DynamicResource Theme.TextMutedBrush}"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="LaterButton"
                    Grid.Column="1"
                    MinWidth="104"
                    Padding="14,8"
                    Margin="0,0,10,0"
                    IsCancel="True"
                    Click="LaterButton_Click"/>
            <Button x:Name="DeleteButton"
                    Grid.Column="2"
                    MinWidth="104"
                    Padding="14,8"
                    Margin="0,0,10,0"
                    Click="DeleteButton_Click"/>
            <Button x:Name="ReuploadButton"
                    Grid.Column="3"
                    MinWidth="132"
                    Padding="14,8"
                    IsDefault="True"
                    Click="ReuploadButton_Click"/>
        </Grid>
    </Grid>
</Window>
